# v0.1.2 Expand Prompt 注入 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 expand 引擎 draft/validate prompt 里的占位符替换成真正的 archetype + 完整风格规约 + 风格样例 + 相邻章节注入，使产出贴合 zhurongshuo 风格。

**Architecture:** orchestrator (`Generate`) 一次性 `loadDraftContext(archetypeID)` 解析内置 archetype/samples/guide，经 `DraftContext` 结构体共享给 draft（全套）和 validate（仅 guide）；相邻章节由 CLI 从 outline 取邻章 abstract 经 `ExpandInput` 传入、注入 user prompt。对齐 outline 引擎 `buildPromptData` 既有可跑模式（整份 archetype 文本 + verbatim samples + archetype-miss 硬失败 / sample-miss 降级）。

**Tech Stack:** Go 1.25+，`text/template`，`//go:embed`（archetypes/style 内置资产），标准 `testing`。

## Global Constraints

- Go 1.25+；module path `github.com/iannil/jianwu`（不改）。
- prompt 模板与文档用中文；代码 identifier 用英文。
- 每个 Task 走 TDD：先写失败测试（RED）→ 最小实现（GREEN）→ commit。
- LLM 输出非确定，**不写 golden file**；可测表面是「prompt 有没有拼进素材」。
- 注入素材均为 `//go:embed` 内置资产，embed 读取运行时不会失败——不为不可能的 embed error 写额外防御；只处理两个查表 miss（archetype-miss 硬失败、sample-miss 降级）。
- 决策依据：`docs/decisions/26-grill-decisions.md` § v0.1.2（Q1/Q4-Q11）。

---

## File Structure

- **Create** `internal/engine/expand/context.go` — `DraftContext` 结构体 + `loadDraftContext` + `marshalArchetype` + `pickSample`。单一职责：把 archetypeID 解析成注入素材。
- **Create** `internal/engine/expand/context_test.go` — context loader 契约测试。
- **Modify** `internal/engine/expand/prompt/system_draft.md.tmpl` — 加 `{{.StyleGuide}}` 槽、删风格冗余 inline 规则（保留任务机制项）。
- **Modify** `internal/engine/expand/prompt/user_draft.md.tmpl` — 加相邻章节段、删尾部「按 schema 输出」陈旧文案。
- **Modify** `internal/engine/expand/prompt/system_validate.md.tmpl` — 加 `{{.StyleGuide}}` 槽、改第 3 条为「依据完整风格规约」。
- **Modify** `internal/engine/expand/iter_draft.go` — 抽 `buildDraftPrompts` 纯函数 + `adjacentContext` 助手；`RunDraft` 新增 `dc DraftContext` 参数。
- **Modify** `internal/engine/expand/iter_draft_test.go` — draft 拼装契约测试。
- **Modify** `internal/engine/expand/iter_validate.go` — 抽 `buildValidatePrompts` 纯函数；`RunValidate` 新增 `styleGuide string` 参数。
- **Modify** `internal/engine/expand/iter_validate_test.go` — validate 拼装契约测试。
- **Modify** `internal/engine/expand/expand.go` — `Generate` 顶部 `loadDraftContext`，dc 传 draft、dc.StyleGuide 传 validate。
- **Modify** `internal/cli/expand.go` — 填 `PreviousChapter`/`NextChapter`（同 Part 邻章）。
- **Modify** `internal/engine/expand/tools.go` + `internal/cli/providers.go` + `internal/engine/expand/tools_test.go` + `internal/cli/providers_test.go` — 删死方法 `ReadAdjacentChapter` + `Outline` 字段（Q6 cleanup）。

---

## Task 1: DraftContext loader

**Files:**
- Create: `internal/engine/expand/context.go`
- Test: `internal/engine/expand/context_test.go`

**Interfaces:**
- Consumes: `archetypes.Load() (map[string]*archetypes.Archetype, error)`、`archetypes.Archetype{ID, Name{Zh,En}, Description, Parts[]{Role,Guidance,ChapterRoleHints}}`、`style.LoadSamples() (map[string]string, error)`、`style.LoadGuide() (string, error)`。
- Produces: `type DraftContext struct { ArchetypeText, SampleText, StyleGuide string }`；`loadDraftContext(archetypeID string) (DraftContext, error)`；`pickSample(map[string]string, string) string`；`marshalArchetype(*archetypes.Archetype) string`。

- [ ] **Step 1: 写失败测试**

```go
// internal/engine/expand/context_test.go
package expand

import (
	"strings"
	"testing"
)

func TestLoadDraftContext_Valid(t *testing.T) {
	dc, err := loadDraftContext("ontology-epistemology-practice")
	if err != nil {
		t.Fatalf("loadDraftContext: %v", err)
	}
	if !strings.Contains(dc.ArchetypeText, "id: ontology-epistemology-practice") {
		t.Errorf("ArchetypeText missing id, got: %q", dc.ArchetypeText)
	}
	if dc.SampleText == "" || dc.SampleText == "(no samples for this archetype)" {
		t.Errorf("SampleText should be the real sample, got: %q", dc.SampleText)
	}
	if !strings.Contains(dc.StyleGuide, "硬规则") {
		t.Errorf("StyleGuide should contain the guide body, got %d bytes", len(dc.StyleGuide))
	}
}

func TestLoadDraftContext_UnknownArchetype(t *testing.T) {
	_, err := loadDraftContext("does-not-exist")
	if err == nil || !strings.Contains(err.Error(), "not found") {
		t.Fatalf("expected 'not found' error, got: %v", err)
	}
}

func TestPickSample(t *testing.T) {
	m := map[string]string{"a": "SAMPLE-A"}
	if got := pickSample(m, "a"); got != "SAMPLE-A" {
		t.Errorf("hit: got %q", got)
	}
	if got := pickSample(m, "z"); got != "(no samples for this archetype)" {
		t.Errorf("miss: got %q", got)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/engine/expand/ -run 'TestLoadDraftContext|TestPickSample' -v`
Expected: FAIL（`undefined: loadDraftContext` 等）。

- [ ] **Step 3: 写最小实现**

```go
// internal/engine/expand/context.go
package expand

import (
	"fmt"

	"github.com/iannil/jianwu/internal/archetypes"
	"github.com/iannil/jianwu/internal/style"
)

// DraftContext holds the resolved injection material for one book's archetype.
// Loaded once per Generate call and shared across draft + validate iterations.
type DraftContext struct {
	ArchetypeText string // compact archetype rendering
	SampleText    string // verbatim samples[id], or degrade sentinel
	StyleGuide    string // full style-guide.md
}

// loadDraftContext resolves archetypeID into injection material.
// Q9: archetype-miss is a hard failure (book meta integrity); sample-miss degrades.
func loadDraftContext(archetypeID string) (DraftContext, error) {
	archs, err := archetypes.Load()
	if err != nil {
		return DraftContext{}, fmt.Errorf("load archetypes: %w", err)
	}
	arch, ok := archs[archetypeID]
	if !ok {
		return DraftContext{}, fmt.Errorf("archetype %q not found", archetypeID)
	}
	samples, err := style.LoadSamples()
	if err != nil {
		return DraftContext{}, fmt.Errorf("load samples: %w", err)
	}
	guide, err := style.LoadGuide()
	if err != nil {
		return DraftContext{}, fmt.Errorf("load style guide: %w", err)
	}
	return DraftContext{
		ArchetypeText: marshalArchetype(arch),
		SampleText:    pickSample(samples, archetypeID),
		StyleGuide:    guide,
	}, nil
}

func pickSample(samples map[string]string, id string) string {
	if s, ok := samples[id]; ok {
		return s
	}
	return "(no samples for this archetype)"
}

// marshalArchetype renders a compact text view for prompt injection.
// Mirrors the scaffolding/outline packages' approach (not a real YAML round-trip).
func marshalArchetype(a *archetypes.Archetype) string {
	out := ""
	out += "id: " + a.ID + "\n"
	out += "name_zh: " + a.Name.Zh + "\n"
	out += "name_en: " + a.Name.En + "\n"
	out += "description: " + a.Description + "\n"
	out += "\nparts:\n"
	for _, p := range a.Parts {
		out += "  - role: " + p.Role + "\n"
		out += "    guidance: " + p.Guidance + "\n"
		if len(p.ChapterRoleHints) > 0 {
			out += "    chapter_role_hints:\n"
			for _, h := range p.ChapterRoleHints {
				out += "      - " + h + "\n"
			}
		}
	}
	return out
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/engine/expand/ -run 'TestLoadDraftContext|TestPickSample' -v`
Expected: PASS（3 个测试全绿）。

- [ ] **Step 5: commit**

```bash
git add internal/engine/expand/context.go internal/engine/expand/context_test.go
git commit -m "feat(expand): add DraftContext loader for archetype/samples/guide injection"
```

---

## Task 2: Draft prompt 注入（templates + buildDraftPrompts）

**Files:**
- Modify: `internal/engine/expand/prompt/system_draft.md.tmpl`
- Modify: `internal/engine/expand/prompt/user_draft.md.tmpl`
- Modify: `internal/engine/expand/iter_draft.go`
- Test: `internal/engine/expand/iter_draft_test.go`

**Interfaces:**
- Consumes: `DraftContext`（Task 1）；`ExpandInput{Language,Length,Topic,Audience,Depth,ChapterTitle,Abstract,KeyConcepts,PreviousChapter,NextChapter}`；`ResearchNotes{Candidates}`；既有助手 `defaultIfEmpty/paragraphHint/wordTarget/joinComma/joinCandidates/jsonMarshalNotes/renderExpand/loadTemplate`；`book.OutlineChapter{Title,Abstract,KeyConcepts}`。
- Produces: `buildDraftPrompts(in ExpandInput, dc DraftContext, notes ResearchNotes) (sys, user string, err error)`；`adjacentContext(label string, ch *book.OutlineChapter) string`；`RunDraft` 新签名 `RunDraft(ctx, chatter, in, dc, notes)`。

- [ ] **Step 1: 写失败测试**

```go
// 追加到 internal/engine/expand/iter_draft_test.go
func TestBuildDraftPrompts_InjectsMaterial(t *testing.T) {
	in := ExpandInput{
		Language:     "zh",
		Length:       "medium",
		Topic:        "测试主题",
		ChapterTitle: "测试章节",
		Abstract:     "本章摘要",
		KeyConcepts:  []string{"概念甲", "概念乙"},
		PreviousChapter: &book.OutlineChapter{
			Title: "前章标题", Abstract: "前章摘要", KeyConcepts: []string{"前概念"},
		},
		NextChapter: &book.OutlineChapter{
			Title: "后章标题", Abstract: "后章摘要", KeyConcepts: []string{"后概念"},
		},
	}
	dc := DraftContext{
		ArchetypeText: "id: test-arch\nparts:\n",
		SampleText:    "SAMPLE-MARKER",
		StyleGuide:    "GUIDE-MARKER",
	}
	sys, user, err := buildDraftPrompts(in, dc, ResearchNotes{Candidates: []string{"https://x"}})
	if err != nil {
		t.Fatalf("buildDraftPrompts: %v", err)
	}
	for _, want := range []string{"id: test-arch", "SAMPLE-MARKER", "GUIDE-MARKER"} {
		if !strings.Contains(sys, want) {
			t.Errorf("system prompt missing %q", want)
		}
	}
	if strings.Contains(sys, "loaded at orchestrator level") {
		t.Error("system prompt still contains old placeholder")
	}
	for _, want := range []string{"前章标题", "前章摘要", "前概念", "后章标题"} {
		if !strings.Contains(user, want) {
			t.Errorf("user prompt missing adjacent material %q", want)
		}
	}
	if strings.Contains(user, "按 schema 输出") {
		t.Error("user prompt still contains stale 'schema' instruction")
	}
}

func TestBuildDraftPrompts_OmitsNilAdjacent(t *testing.T) {
	in := ExpandInput{Language: "zh", Length: "medium", ChapterTitle: "c"}
	_, user, err := buildDraftPrompts(in, DraftContext{}, ResearchNotes{})
	if err != nil {
		t.Fatalf("buildDraftPrompts: %v", err)
	}
	if strings.Contains(user, "相邻章节") {
		t.Error("nil adjacent should omit the 相邻章节 section")
	}
}
```

> 确保 test 文件 import 含 `"strings"` 与 `"github.com/iannil/jianwu/internal/book"`。

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/engine/expand/ -run 'TestBuildDraftPrompts' -v`
Expected: FAIL（`undefined: buildDraftPrompts`）。

- [ ] **Step 3a: 改 system_draft 模板**（删风格冗余规则、留任务机制、加 guide 槽）

`internal/engine/expand/prompt/system_draft.md.tmpl` 全文替换为：

```
你是 jianwu 的 expand 写作助手。基于章节上下文 + 研究笔记，撰写章节正文。

## 任务要求

1. 用 {{.Language}} 撰写。
2. 每个事实性陈述必须有引用脚注 [^N]。脚注 URL 必须来自 candidates 列表。
3. 段落长度：{{.ParagraphHint}}。
4. 章节字数目标：{{.WordTarget}} 字。
5. 输出纯 markdown 正文，结尾列出脚注（[^1]: [Title](url) accessed DATE）。

## 风格规约（唯一风格真相源，严格遵守，尤其「反例集」与「自检清单」）

{{.StyleGuide}}

## 风格样例（模仿语调、句式节奏、概念处理）

{{.Samples}}

## 章节骨架（archetype）

{{.Archetype}}
```

- [ ] **Step 3b: 改 user_draft 模板**（加相邻章节、删「按 schema 输出」）

`internal/engine/expand/prompt/user_draft.md.tmpl` 全文替换为：

```
为以下章节撰写正文。

主题：{{.Topic}}
受众：{{.Audience}}
深度：{{.Depth}}
章节：{{.ChapterTitle}}
abstract: {{.Abstract}}
key_concepts: {{.KeyConcepts}}
{{if .PrevContext}}
相邻章节（保持连贯：勿重复上一章已立概念，为下一章留接口）：
{{.PrevContext}}
{{end}}{{if .NextContext}}{{.NextContext}}
{{end}}
研究笔记：
{{.ResearchNotes}}

候选 URL（脚注必须从这里选）：
{{.Candidates}}

直接输出正文 markdown，结尾附脚注。
```

- [ ] **Step 3c: 改 iter_draft.go**（抽 buildDraftPrompts + adjacentContext，RunDraft 加 dc 参数）

把 `iter_draft.go` 的 `RunDraft` 函数体替换为下面两个函数（保留文件内既有 helper 不动）：

```go
// RunDraft executes iteration 2: LLM writes chapter prose with [^N] footnotes.
func RunDraft(
	ctx context.Context,
	chatter llm.Chatter,
	in ExpandInput,
	dc DraftContext,
	notes ResearchNotes,
) (string, error) {
	sys, user, err := buildDraftPrompts(in, dc, notes)
	if err != nil {
		return "", err
	}
	resp, err := chatter.Chat(ctx, llm.ChatRequest{
		Messages: []llm.Message{
			{Role: "system", Content: sys},
			{Role: "user", Content: user},
		},
	})
	if err != nil {
		return "", fmt.Errorf("draft llm chat: %w", err)
	}
	return resp.Content, nil
}

// buildDraftPrompts renders the draft system + user prompts. Pure (no I/O beyond embed),
// so it is unit-testable without an LLM (Q10).
func buildDraftPrompts(in ExpandInput, dc DraftContext, notes ResearchNotes) (string, string, error) {
	sysBytes, _ := loadTemplate("system_draft")
	sys, err := renderExpand("system_draft", sysBytes, map[string]any{
		"Language":      defaultIfEmpty(in.Language, "zh"),
		"ParagraphHint": paragraphHint(in.Length),
		"WordTarget":    wordTarget(in.Length),
		"StyleGuide":    dc.StyleGuide,
		"Samples":       dc.SampleText,
		"Archetype":     dc.ArchetypeText,
	})
	if err != nil {
		return "", "", err
	}
	notesJSON, _ := jsonMarshalNotes(notes)
	userBytes, _ := loadTemplate("user_draft")
	user, err := renderExpand("user_draft", userBytes, map[string]any{
		"Topic":         in.Topic,
		"Audience":      in.Audience,
		"Depth":         in.Depth,
		"ChapterTitle":  in.ChapterTitle,
		"Abstract":      in.Abstract,
		"KeyConcepts":   joinComma(in.KeyConcepts),
		"ResearchNotes": notesJSON,
		"Candidates":    joinCandidates(notes.Candidates),
		"PrevContext":   adjacentContext("上一章", in.PreviousChapter),
		"NextContext":   adjacentContext("下一章", in.NextChapter),
	})
	if err != nil {
		return "", "", err
	}
	return sys, user, nil
}

// adjacentContext renders one neighbor chapter for coherence, or "" when nil (Q5).
func adjacentContext(label string, ch *book.OutlineChapter) string {
	if ch == nil {
		return ""
	}
	s := label + "《" + ch.Title + "》\n摘要：" + ch.Abstract
	if len(ch.KeyConcepts) > 0 {
		s += "\n关键概念：" + joinComma(ch.KeyConcepts)
	}
	return s
}
```

在 `iter_draft.go` 的 import 块加入 `"github.com/iannil/jianwu/internal/book"`。

- [ ] **Step 3d: 更新既有 RunDraft 调用点**

`grep -rn "RunDraft(" internal/engine/expand/` 找到现有调用（`expand.go` + 任何 `*_test.go`），给每处补 `DraftContext{}`（测试里）或真实 dc（`expand.go`，Task 3 处理）。本步只让既有测试编译：测试里的 `RunDraft(ctx, chatter, in, notes)` 改为 `RunDraft(ctx, chatter, in, DraftContext{}, notes)`。

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/engine/expand/ -run 'TestBuildDraftPrompts' -v && go test ./internal/engine/expand/`
Expected: PASS（新契约测试绿 + 包内既有测试不回归）。

- [ ] **Step 5: commit**

```bash
git add internal/engine/expand/prompt/system_draft.md.tmpl internal/engine/expand/prompt/user_draft.md.tmpl internal/engine/expand/iter_draft.go internal/engine/expand/iter_draft_test.go
git commit -m "feat(expand): inject archetype/guide/samples/adjacent into draft prompt"
```

---

## Task 3: Generate 装配 + CLI 填相邻章节

**Files:**
- Modify: `internal/engine/expand/expand.go`
- Modify: `internal/cli/expand.go`
- Test: `internal/engine/expand/expand_test.go`（追加）

**Interfaces:**
- Consumes: `loadDraftContext`（Task 1）、新签名 `RunDraft`（Task 2）、`findChapter(outline, partIdx, chIdx) (*book.OutlineChapter, error)`（既有，`cli/expand.go:186`）。
- Produces: `Generate` 内部 dc 装配（签名不变：`Generate(ctx, chatter, tools, in) (*ExpandOutput, error)`）；CLI 填 `expandIn.PreviousChapter/NextChapter`。

- [ ] **Step 1: 写失败测试**（archetype-miss 在 Generate 顶部 fail-fast）

```go
// 追加到 internal/engine/expand/expand_test.go
func TestGenerate_BadArchetypeFailsFast(t *testing.T) {
	in := ExpandInput{ArchetypeID: "nonexistent-archetype", ChapterTitle: "c"}
	_, err := Generate(context.Background(), &stubChatter{}, nil, in)
	if err == nil || !strings.Contains(err.Error(), "not found") {
		t.Fatalf("expected archetype 'not found' before any LLM call, got: %v", err)
	}
}
```

> 复用包内既有 mock chatter；若名字不同，改成现有那个（`grep -n "llm.Chatter" internal/engine/expand/*_test.go` 查）。确保 import 含 `"context"`、`"strings"`。

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/engine/expand/ -run 'TestGenerate_BadArchetype' -v`
Expected: FAIL（当前 Generate 不 load archetype，错误信息不含 "not found"）。

- [ ] **Step 3a: 改 expand.go 的 Generate**

在 `Generate` 函数体最前面（iter 1 之前）加：

```go
	// Resolve injection material once; archetype-miss fails fast (Q1, Q9).
	dc, err := loadDraftContext(in.ArchetypeID)
	if err != nil {
		return nil, fmt.Errorf("load draft context: %w", err)
	}
```

并把 iter 2 调用从 `RunDraft(ctx, chatter, in, notes)` 改为 `RunDraft(ctx, chatter, in, dc, notes)`。
（iter 3 的 validate 在 Task 4 改；本 Task 先让 `dc` 用于 draft，validate 暂保持原状——`dc` 已被 draft 使用，不会触发 unused。）

- [ ] **Step 3b: 改 cli/expand.go 填相邻章节**

在 `expandIn := expand.ExpandInput{...}`（`cli/expand.go:115`）之后、`expand.Generate` 调用之前插入：

```go
	// Adjacent chapters (same Part) for coherence; nil at Part boundaries (Q5).
	if prev, perr := findChapter(outline, partIdx, chIdx-1); perr == nil {
		expandIn.PreviousChapter = prev
	}
	if next, nerr := findChapter(outline, partIdx, chIdx+1); nerr == nil {
		expandIn.NextChapter = next
	}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/engine/expand/ -run 'TestGenerate_BadArchetype' -v && go test ./internal/engine/expand/ ./internal/cli/`
Expected: PASS（fail-fast 测试绿；expand + cli 包既有测试不回归，含 mock-chatter e2e）。

- [ ] **Step 5: commit**

```bash
git add internal/engine/expand/expand.go internal/engine/expand/expand_test.go internal/cli/expand.go
git commit -m "feat(expand): wire DraftContext in Generate + populate adjacent chapters in CLI"
```

---

## Task 4: Validate guide 注入

**Files:**
- Modify: `internal/engine/expand/prompt/system_validate.md.tmpl`
- Modify: `internal/engine/expand/iter_validate.go`
- Modify: `internal/engine/expand/expand.go`
- Test: `internal/engine/expand/iter_validate_test.go`（追加）

**Interfaces:**
- Consumes: `DraftContext.StyleGuide`（Task 1）、既有 `JSONSchemaValidation/renderExpand/loadTemplate/jsonMarshalNotes`。
- Produces: `buildValidatePrompts(draft string, notes ResearchNotes, styleGuide string) (sys, user string, err error)`；`RunValidate` 新签名 `RunValidate(ctx, chatter, draft, notes, styleGuide)`。

- [ ] **Step 1: 写失败测试**

```go
// 追加到 internal/engine/expand/iter_validate_test.go
func TestBuildValidatePrompts_InjectsGuide(t *testing.T) {
	sys, _, err := buildValidatePrompts("草稿", ResearchNotes{}, "VALIDATE-GUIDE-MARKER")
	if err != nil {
		t.Fatalf("buildValidatePrompts: %v", err)
	}
	if !strings.Contains(sys, "VALIDATE-GUIDE-MARKER") {
		t.Error("validate system prompt missing injected style guide")
	}
	if strings.Contains(sys, "无空话、无 emoji、术语标「」") {
		t.Error("validate should drop the old inline 3-item shorthand")
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/engine/expand/ -run 'TestBuildValidatePrompts' -v`
Expected: FAIL（`undefined: buildValidatePrompts`）。

- [ ] **Step 3a: 改 system_validate 模板**

`internal/engine/expand/prompt/system_validate.md.tmpl` 全文替换为：

```
你是 jianwu 的 expand 校验助手。基于草稿 + 原始研究笔记，自检并修订。

## 输出格式

JSON Schema:
- revised_markdown: 修订后的正文（纯 markdown + 脚注）
- claims: 数组，每条 { text, has_citation }

## 校验清单

1. 每个事实性陈述是否有 [^N] 脚注？没有的标 has_citation=false。
2. 引用是否准确（URL 真的支撑了该陈述）？
3. 风格是否合规？依据下方完整风格规约，尤其「四、反例集」与「五、自检清单」逐条核对。
4. 段落是否连贯？
5. 字数是否达标？

## 风格规约（校验依据）

{{.StyleGuide}}

修订后输出。如果原稿已经合格，revised_markdown 可以与原稿相同。
```

- [ ] **Step 3b: 改 iter_validate.go**（抽 buildValidatePrompts + RunValidate 加 styleGuide 参数）

把 `RunValidate` 替换为：

```go
// RunValidate executes iteration 3: LLM self-checks and revises the draft.
func RunValidate(
	ctx context.Context,
	chatter llm.Chatter,
	draft string,
	notes ResearchNotes,
	styleGuide string,
) (ValidationResult, error) {
	sys, user, err := buildValidatePrompts(draft, notes, styleGuide)
	if err != nil {
		return ValidationResult{}, err
	}
	schema, _ := JSONSchemaValidation()
	resp, err := chatter.Chat(ctx, llm.ChatRequest{
		Messages: []llm.Message{
			{Role: "system", Content: sys},
			{Role: "user", Content: user},
		},
		JSONSchema: schema,
	})
	if err != nil {
		return ValidationResult{}, fmt.Errorf("validate llm chat: %w", err)
	}
	var result ValidationResult
	if err := json.Unmarshal([]byte(resp.Content), &result); err != nil {
		return ValidationResult{}, fmt.Errorf("parse validation result: %w (content: %s)", err, truncate(resp.Content, 500))
	}
	return result, nil
}

// buildValidatePrompts renders the validate system + user prompts. Pure, unit-testable (Q10).
func buildValidatePrompts(draft string, notes ResearchNotes, styleGuide string) (string, string, error) {
	sysBytes, _ := loadTemplate("system_validate")
	sys, err := renderExpand("system_validate", sysBytes, map[string]any{
		"StyleGuide": styleGuide,
	})
	if err != nil {
		return "", "", err
	}
	notesJSON, _ := jsonMarshalNotes(notes)
	userBytes, _ := loadTemplate("user_validate")
	user, err := renderExpand("user_validate", userBytes, map[string]any{
		"Draft":         draft,
		"ResearchNotes": notesJSON,
	})
	if err != nil {
		return "", "", err
	}
	return sys, user, nil
}
```

- [ ] **Step 3c: 改 expand.go 传 guide 给 validate**

把 iter 3 调用从 `RunValidate(ctx, chatter, draft, notes)` 改为 `RunValidate(ctx, chatter, draft, notes, dc.StyleGuide)`。

- [ ] **Step 3d: 更新既有 RunValidate 测试调用**

`grep -rn "RunValidate(" internal/engine/expand/*_test.go` 找到的调用补 `""` 作 styleGuide 末参。

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/engine/expand/ -run 'TestBuildValidatePrompts' -v && go test ./internal/engine/expand/`
Expected: PASS。

- [ ] **Step 5: commit**

```bash
git add internal/engine/expand/prompt/system_validate.md.tmpl internal/engine/expand/iter_validate.go internal/engine/expand/expand.go internal/engine/expand/iter_validate_test.go
git commit -m "feat(expand): inject style guide into validate prompt as shared style authority"
```

---

## Task 5: Cleanup — 删死方法 ReadAdjacentChapter + Outline 字段

> 独立关注点（Q6 cleanup，memory Q15 风格：单独 commit）。被 Q5 静态注入取代。

**Files:**
- Modify: `internal/engine/expand/tools.go`
- Modify: `internal/cli/providers.go`
- Modify: `internal/engine/expand/tools_test.go`
- Modify: `internal/cli/providers_test.go`

**Interfaces:**
- Produces: `NewToolRegistry(s, r, e) *ToolRegistry`（删第 4 个 `outlineFn` 参数）；`buildToolRegistry(deps *ProviderDeps) (*expand.ToolRegistry, error)`（删 `outline` 参数）。

- [ ] **Step 1: 删 tools.go 的死代码**

在 `internal/engine/expand/tools.go`：
- 删 `ToolRegistry` 结构体里的 `Outline func(partIdx, chIdx int) (string, error)` 字段（约 line 20）。
- 删 `NewToolRegistry` 的 `outlineFn func(...)` 参数及函数体里的 `Outline: outlineFn,` 赋值（约 line 33-46）。
- 删整个 `ReadAdjacentChapter` 方法（约 line 88-94）。

改后 `NewToolRegistry` 签名为：

```go
func NewToolRegistry(
	s search.Searcher,
	r reader.Reader,
	e llm.Embedder,
) *ToolRegistry {
	return &ToolRegistry{
		Searcher:  s,
		Reader:    r,
		Embedder:  e,
		citations: map[string]Citation{},
	}
}
```

- [ ] **Step 2: 改 providers.go 的 buildToolRegistry**

`internal/cli/providers.go` 的 `buildToolRegistry` 整体替换为：

```go
// buildToolRegistry assembles an expand.ToolRegistry from ProviderDeps.
func buildToolRegistry(deps *ProviderDeps) (*expand.ToolRegistry, error) {
	if deps == nil {
		return nil, fmt.Errorf("deps is nil")
	}
	return expand.NewToolRegistry(deps.Searcher, deps.Reader, deps.Embedder), nil
}
```

- [ ] **Step 3: 更新 buildToolRegistry / NewToolRegistry 调用点**

- `internal/cli/expand.go:109`：`buildToolRegistry(deps, outline)` → `buildToolRegistry(deps)`。
- `internal/cli/providers_test.go:109`：同上改 `buildToolRegistry(deps)`；若该测试里 `outline` 变量（约 line 103）就此 unused，一并删除其声明。
- `grep -rn "NewToolRegistry(" internal/engine/expand/*_test.go`：每处去掉第 4 个 `outlineFn` 实参。

- [ ] **Step 4: 删 tools_test.go 的死测试**

删 `internal/engine/expand/tools_test.go` 里 `TestReadAdjacentChapter`（约 line 265）和 `TestReadAdjacentChapterWithoutOutline`（约 line 285）两个函数，以及它们局部的 `outlineFn` 定义。

- [ ] **Step 5: 跑全量测试确认无回归**

Run: `go build ./... && go test ./...`
Expected: 全绿（23 包），无 unused/编译错误。

- [ ] **Step 6: commit**

```bash
git add internal/engine/expand/tools.go internal/cli/providers.go internal/engine/expand/tools_test.go internal/cli/providers_test.go internal/cli/expand.go
git commit -m "refactor(expand): remove dead ReadAdjacentChapter + Outline field (superseded by static injection)"
```

---

## Final Verification

- [ ] `go build ./... && go test ./...` 全绿。
- [ ] `grep -rn "loaded at orchestrator level\|按 schema 输出" internal/engine/expand/` 无残留。
- [ ] 人读验收（非 CI）：跑一章真实 expand（`jianwu expand <slug> <NN-MM>`），祝融读产出判断是否贴近 zhurongshuo 风格——这是 scope 的真验收。
- [ ] 更新 `docs/PROJECT_STATUS.md`：把 v0.1.2 从"未做/占位符"标记为已完成，移除 §（占位符质量缺口）相关条目。

---

## Self-Review Notes

- **Spec coverage**：Q1(Task3 Generate load)、Q4(Task2/4 guide+删inline)、Q5(Task2 adjacentContext+Task3 CLI填邻章)、Q6(Task5 cleanup+similar-book 不在本 plan)、Q7(Task2 draft全套/Task4 validate仅guide/research不动)、Q8(模板 system=guide+samples+archetype, user=本章+邻章)、Q9(Task1 fail/degrade)、Q10(buildDraftPrompts/buildValidatePrompts 纯函数契约测试)、Q11(marshalArchetype 整份 + verbatim sample) 均有对应 Task。
- **类型一致**：`DraftContext{ArchetypeText,SampleText,StyleGuide}` 全程一致；`RunDraft(...,dc,notes)`、`RunValidate(...,styleGuide)` 新签名在 Task2/3/4 调用点一致更新。
- **范围外**（按决策）：similar-book（Q6 切出独立切片）、archetype 第三节裁剪/sample 剥头（Q11 降级为验收暴露问题再做）、scaffolding 注入（v0.1.2 只动 expand）。
